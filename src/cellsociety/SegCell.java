package cellsociety;

import java.util.ArrayList;


public class SegCell extends Cell{

    public SegCell(int row, int col,int numRows, int numCols, State state) {
        super(row, col, numRows, numCols, state);
    }
}
